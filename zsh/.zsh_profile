VIM="nvim"

addToPathFront() {
    if [[ "$PATH" != *"$1"* ]]; then
        export PATH=$1:$PATH
    fi
}

addToPathFront $HOME/.local/scripts

bindkey -s ^f "~/dotfile_testing/tmux-sessionizer.sh\n"
